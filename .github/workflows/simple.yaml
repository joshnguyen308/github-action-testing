name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    step:
      - name: echo a string
        run: echo "hello world"
      - name: multiline
        run: |
          node -v
          npm -v
          python -v
